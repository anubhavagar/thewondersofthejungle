# App Settings
PROJECT_NAME="The Wonders of the Jungle"

# DB Settings
# Hybrid: Falls back to local SQLite (api/health_app.db) if not provided
# POSTGRES_URL="postgres://user:password@localhost:5432/db"

# Vercel Blob (For image storage)
# Required for media analysis functionality
BLOB_READ_WRITE_TOKEN="your_vercel_blob_token_here"

# OTP Settings (Simulated)
OTP_EXPIRY_MINUTES=60
OTP_LENGTH=4
OTP_SENDER_EMAIL="anubhavagar@gmail.com"
